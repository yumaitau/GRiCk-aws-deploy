apiVersion: v1
kind: Service
metadata:
  name: {{ include "grick.fullname" . }}-web
  labels:
    {{- include "grick.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grick.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
