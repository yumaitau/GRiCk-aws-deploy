apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grick.fullname" . }}-config
  labels:
    {{- include "grick.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
